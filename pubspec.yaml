name: hooplytics
description: "Hooplytics – Twoje centrum statystyk rzutowych 🏀"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  provider: ^6.1.2
  supabase_flutter: ^2.8.4
  google_sign_in: ^6.2.2
  speech_to_text: ^7.3.0
  speech_to_text_windows: ^1.0.0+beta.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/avatars/